[env:teensy41]
platform = teensy
board = teensy41
framework = arduino
build_flags = -DUSB_SERIAL
lib_deps =
    # Library dependencies not yet fully supported for Teensy platform
    # The LibraryCompiler needs to be made platform-agnostic (currently AVR-specific)
    # https://github.com/PaulStoffregen/Time
